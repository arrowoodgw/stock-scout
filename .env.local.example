# =============================================================================
# Stock Scout — Environment Variable Reference
# =============================================================================
# Copy this file to .env.local and fill in your own values.
#   cp .env.local.example .env.local
#
# .env.local is listed in .gitignore and must NEVER be committed to source control.
# =============================================================================

# Polygon.io API key — sign up free at https://polygon.io
# Required when DATA_MODE=real for live stock price and historical data.
POLYGON_API_KEY=your_polygon_api_key_here

# SEC EDGAR user agent — use your real name and email per SEC fair access policy:
# https://www.sec.gov/os/accessing-edgar-data
# Required when DATA_MODE=real for fundamental data (P/E, revenue, margins, etc.).
SEC_USER_AGENT=Your Name your@email.com

# Data mode (server-side) — set to "real" to use live data, "mock" for development
# without API keys. Mock mode uses deterministic seeded data and requires no keys.
DATA_MODE=mock

# Data mode (client-side) — must match DATA_MODE above.
NEXT_PUBLIC_DATA_MODE=mock
